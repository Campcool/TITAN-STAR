# TITAN-STAR — 設計初衷與內容說明（供 AI 分析用）

> 本文件是給「其他 AI 模型」閱讀的專案說明書。目的是讓沒有看過原始碼的 AI
> 也能在幾分鐘內完整理解：這個專案要解決什麼問題、領域知識是什麼、系統如何
> 設計、資料長什麼樣、有哪些分析引擎、以及目前的限制與待辦。閱讀本文後，
> 即可對程式碼、報表內容或改善方向提出有根據的分析。

---

## 0. 一句話總結

TITAN-STAR 是一個**純前端（vanilla JS、無後端）的電子工廠維修數據分析報表**，
把每月維修 Excel 上傳後，在瀏覽器內即時做出多維度品質分析（Pareto、SPC、FMEA、
CAPA、COPQ、批次追溯、預測），並以**「主管角色視角」**把雜亂的發現整理成各級
主管各自應追蹤的摘要。部署在 GitHub Pages。

- 線上版：`https://campcool.github.io/TITAN-STAR/`
- 主要使用者：50–60 歲的工廠主管／品保人員（因此特別重視大字級與行動裝置體驗）

---

## 1. 設計初衷（為什麼做這個）

工廠原本用 Excel 逐月記錄維修，痛點是：

1. **看不出趨勢與根因**：單月 Excel 無法回答「哪個零件一直壞」「哪一批料有問題」
   「這個月比上個月惡化還是改善」。
2. **資訊雜湊、各管各的**：同一份報表，董事長想看錢、品保想看 CAPA、採購想看來料、
   研發想看設計缺陷——但沒有人能快速找到「該我管的那幾件事」。
3. **責任落點不清**：一顆器材壞了，到底是**原廠出廠就有問題**，還是**整新（翻修）
   過程沒做好**？沒有資料佐證就會互相推諉。
4. **無品質工程方法**：缺少 SPC 管制圖、FMEA 風險量化、CAPA 改善閉環、COPQ 成本量化
   這些製造業標準工具。

因此本專案的設計目標是：**把原始維修記錄，自動轉換成「可究責、可追蹤、可決策」的
品質情報**，而且要讓每一個層級的主管都能「一眼看到自己該追的事」。

---

## 2. 核心領域知識（最重要，分析前必讀）

這是本專案區別於一般「報表工具」的關鍵，也是 AI 分析時最容易忽略的部分。

### 2.1 製令 vs 製造日期（雙批次維度）

每一台器材上有兩個關鍵日期欄位，意義完全不同：

| 欄位 | 中文 | 意義 | 是否會變 |
|------|------|------|----------|
| **製令** | 出廠批次 / 身分證 | 器材**原始出廠**的年月批次，格式 `YYMMDD + 3碼序號`（YY 為西元 2 位年），例如 `250410057` = 2025 年 04 月、批次序 057。**終生不變**。 | 永不改變 |
| **製造日期** | 整新日期 | 若客戶回收、工廠**整新（翻修）**後，會**重貼一個新的製造日期**。 | 整新時會重貼 |

### 2.2 全新 vs 整新（責任落點的關鍵判定）

- **全新（brandNew）**：`製造日期的年月 == 製令的年月` → 這台從沒被翻修過。
- **整新（refurb）**：`製造日期的年月 != 製令的年月` → 這台被翻修過（製造日期較新）。

**這直接決定故障責任歸屬：**

- 全新品故障 → **原廠／來料／原始生產批次**的責任（找製令那一批的元件與產線）。
- 整新品故障 → **整新製程／整新後驗收**的責任（找翻修那條線）。

對應的程式判定在 `parser.js` 的 `classifyCondition(mfgMonth, orderMonth)`。

### 2.3 兩種批次分析

因此系統做**兩個維度**的批次集中度分析：

1. **出廠批次（依製令）**：故障集中在某個製令年月 → 該批**原始元件／製程**有系統性瑕疵。
   （`originBatchPareto` = 元件瑕疵落點分析）
2. **製造批次（依製造日期）**：故障集中在某個製造年月 → 某個**整新梯次**或生產梯次有問題。

### 2.4 目前的資料限制（非常重要）

> **目前 1955 筆記錄中，沒有任何一筆同時具備「製令 + 製造日期」**，因為工廠是
> **陸續**在 3、4 月才開始補上「製令」欄位（目前只有 ZSPMG31 機種的工作表有製令）。
> 因此 `condition`（全新/整新）目前**全部為空字串 `''`**，全新/整新分析**尚未實際生效**，
> 系統已準備好，等工廠把製令欄位補齊到所有工作表後即自動分類。
>
> AI 分析時請注意：不要把「全新/整新分析顯示空白」當成 bug，這是**資料尚未齊備**的
> 預期狀態，UI 上已有醒目提示引導工廠補件。

---

## 3. 技術架構

### 3.1 技術棧

- **純前端 vanilla JavaScript**，無框架、無後端、無建置工具鏈（除了自寫的 `build.js`）。
- 圖表：Chart.js 4.4（CDN）。
- Excel 解析：SheetJS / xlsx 0.18（CDN）。
- 字型：Inter + Noto Sans TC + JetBrains Mono（Google Fonts）。
- 資料持久化：`localStorage`（本機）+ `data.json`（雲端同步的快照）。
- 部署：GitHub Pages，從 `main` 分支根目錄直接發佈（無 CI workflow）。

### 3.2 檔案結構與職責

| 檔案 | 行/大小 | 職責 |
|------|---------|------|
| `index.html` | ~32 KB | 單頁 SPA 的所有 DOM 骨架（登入、上傳、儀表板、側欄、各分析頁、RMA） |
| `styles.css` | ~83 KB | 主視覺系統（深色、無障礙字級、RWD） |
| `rma-styles.css` | ~19 KB | RMA 管理模組的樣式 |
| `parser.js` | ~19 KB | **Excel → 標準化記錄**：欄位別名對應、機種/大類正規化、製令/製造日期解析、零件品號大類對應 |
| `analyzer.js` | ~41 KB | **純函式分析引擎**（25 個 export，見下節），不碰 DOM |
| `app.js` | ~214 KB | **主應用**：狀態、路由、所有頁面渲染、角色系統、摘要引擎、圖表、互動 |
| `report.js` | ~25 KB | 產生可下載的 HTML 報告 |
| `rma.js` | ~65 KB | RMA（退貨維修授權）管理子系統（獨立模組） |
| `build.js` | ~3.5 KB | 把上述 JS/CSS 內嵌打包成單一可離線的 `TITAN-STAR.html` |
| `TITAN-STAR.html` | ~487 KB | **打包產物**（離線單檔版，9 個 script 區塊） |
| `data.json` | ~836 KB | 雲端資料快照（2026-03、2026-04 共 1955 筆 + capa/costCfg） |

### 3.3 兩個執行版本

- **線上版**：`index.html` 透過 `<script src="...">` 載入各別 JS/CSS（GitHub Pages 服務的版本）。
- **離線版**：`TITAN-STAR.html` 是 `build.js` 把所有資源內嵌的單一檔案，可離線雙擊開啟。
- **修改原始碼後必須 `node build.js` 重新打包**，否則離線版會過時。

### 3.4 全域命名空間

各模組掛在 `window` 上：`RepairParser`、`RepairAnalyzer`、`App`、`RepairReport`、
`RepairDB`、`Auth`、`RMA`。`app.js` 以 `window.App = (function(){ ... })()` 的 IIFE 封裝。

---

## 4. 資料模型

### 4.1 `data.json` 結構

```
{
  "months": {
    "2026-03": { "records": [ <record>, ... ] },   // 1084 筆
    "2026-04": { "records": [ <record>, ... ] }     //  871 筆
  },
  "capa": [],          // CAPA 改善案清單（目前空）
  "costCfg": null      // 成本參數（目前未設定 → COPQ 待啟用）
}
```

### 4.2 單筆 record 欄位

```
sheet      工作表來源名稱
date       維修/檢修日期
model      正規化後機種代碼（如 SCX0051、IOT0600、ZSPMG31）
modelRaw   原始機種字串
category   大類（監視器/傳統保全/無線保全/車機系統/AED/門禁/其他）
batch      製令原字串
serial     序號
reason     正規化故障原因
reasonRaw  原始故障原因字串
content    維修內容/故障描述自由文字
isScrap    是否報廢（boolean）
part1~3    更換零件名稱（最多 3 個）
qty1~3     對應數量
part1~3Norm 正規化零件名
mfg        製造日期 → 正規化為 YYYY-MM
orderMonth 製令 → 解析出的出廠年月 YYYY-MM
condition  '全新' | '整新' | ''（目前全為 ''，見 §2.4）
```

---

## 5. 分析引擎（`analyzer.js` 的 25 個純函式）

全部是 `(records/db, opts) → 結果物件` 的純函式，不依賴 DOM，可獨立測試。

- **基礎統計**：`computeKPIs`、`getRecords`、`getDenominators`、`monthlyTrend`、`partTrend`
- **Pareto / 排行**：`partPareto`、`modelRank`、`aggregateParts`、`reasonBreakdown`、
  `componentCategoryPareto`（依零件品號大類分群）
- **重複/跨期**：`repeatedSerials`、`crossMonthSerials`（同序號跨月再進廠 = 未根治）、
  `crossModelParts`
- **品質工程**：
  - `spcAnalysis` — SPC p 管制圖（UCL/LCL，找特殊原因點）
  - `fmeaAnalysis` — FMEA 風險（RPN = 嚴重度 S × 發生度 O × 偵測度 D）
  - `rootCauseTree` / `classifyFault` — 根因分類樹
  - `detectAnomalies` — 異常偵測（用量暴增、報廢集中、重複故障…）
  - `qualityMetrics` — 良率/報廢率等綜合指標
- **預測**：`forecastNextMonth`（線性回歸 + 3 個月移動平均）
- **成本**：`costAnalysis`（COPQ = 報廢成本 + 工時成本，需 costCfg）
- **批次/究責**（本專案特色，見 §2）：
  - `batchAnalysis` — 雙維度批次分析，產生 4 個旗標：
    `出廠批次集中`、`製造批次集中`、`全新早夭`、`整新後即壞`
  - `conditionSummary` — 全新/整新數量與各自報廢率
  - `originBatchPareto` — 依製令出廠年月的故障 Pareto（元件瑕疵落點）

---

## 6. 角色視角系統（本專案的核心 UX 概念）

### 6.1 13 種角色

`all`(綜合)、`ceo`(董事長)、`factory`(廠長)、`procure`(採購)、`prod`(生產)、
`qa`(品檢)、`logistics`(物流)、`cs`(客服)、`repair`(維修)、`hw`(硬體研發)、
`fw`(韌體研發)、`finance`(財務)、`sales`(業務)。

### 6.2 運作方式

- 頁面頂端有**全站角色切換列**（sticky），切換後**整個報表都會以該角色視角重繪**
  （不只某一頁）。
- **`gatherFindings()`**（在 `app.js`）是摘要引擎核心：它從 ~11 個來源（異常、批次旗標、
  出廠批次瑕疵、FMEA、CAPA 逾期、跨月重複、COPQ、零件大類、高故障機種、報廢率、
  韌體訊號）蒐集「發現」，每個發現是一個物件：
  ```
  { sev, area, icon, title, detail, action, page, roles[] }
  ```
  並標註**這個發現和哪些角色相關**（`roles`）。
- **`summaryForRole(role)`** 過濾出該角色相關的發現，依嚴重度排序。
- **「★ 主管摘要」頁**把結果分三層顯示：**應立即追蹤(critical) / 本期應關注(warn) /
  持續監控(info)**，每張卡片附「💡建議行動」與「前往 →」下鑽連結。

> 設計意圖：解決「資訊雜湊」——每位主管打開摘要頁，看到的是**為他量身過濾、排好序的
> 待辦清單**，而非一堆通用圖表。

---

## 7. 頁面清單（14 個分析頁 + RMA 模組）

`summary`(主管摘要)、`overview`(總覽)、`alerts`(異常偵測)、`parts`(零件 Pareto)、
`cross`(跨機種矩陣)、`trend`(月份趨勢)、`reason`(故障原因)、`quality`(品質指標/SPC)、
`batch`(製造批次分析)、`risk`(風險/根因/預測 FMEA)、`capa`(CAPA 追蹤)、`cost`(成本量化
COPQ)、`scrap`(報廢&重修)、`detail`(明細資料)。

另有獨立的 **RMA 管理模組**（`rma.js`），處理退貨維修授權流程，與分析模組以 mode-bar 切換。

每頁都有 `HELP` 物件提供「這頁在算什麼／怎麼解讀／誰該看／KPI 公式」的內建說明。

---

## 8. 無障礙與 RWD（因使用者為長輩）

- **顯示大小切換**（標題列）：標準/大/特大，透過 `html[data-fontscale]` + `body { zoom }`
  全站等比縮放，**預設「大」**；設定存 `localStorage`，在繪製前套用避免閃爍。
- 基礎字級 19px，最小字級已整體上調。
- **行動裝置導覽**：≤1100px 時側欄改為**漢堡選單 + 滑出抽屜 + 遮罩**（修復了先前
  「側欄直接 display:none 導致手機完全無導覽」的重大缺陷）。
- 手機斷點：KPI 單欄、加大觸控目標、隱藏次要按鈕。

---

## 9. 目前已知限制與待辦（給 AI 分析時的重點）

1. **製令尚未補齊**：`condition` 全空，全新/整新分析與出廠批次究責**尚未實際生效**
   （資料問題，非程式問題）。等工廠把製令補到所有工作表即自動運作。
2. **COPQ 未設定**：`costCfg: null`，財務視角需用「快速估算模式」填 3 個概略單價才會有數字。
3. **CAPA 為空**：`capa: []`，品質改善閉環尚未開始使用。
4. **FMEA 分類待精進**：「其他/未分類」常是最高 RPN，代表零件品號對照表（`PART_CATEGORY`，
   54 類）尚未涵蓋所有品號。
5. **無自動化測試框架**：驗證靠手寫的 node headless 腳本 eval `parser.js`/`analyzer.js`。
6. **`app.js` 過大**（~214 KB 單檔），未模組化。
7. 資料目前僅 2 個月（2026-03、2026-04），趨勢/預測樣本數少。

---

## 10. 如何驗證與打包（給要動程式碼的 AI）

- **語法檢查**：`node --check app.js`（analyzer.js、parser.js 同）。
- **引擎煙霧測試**：用 node stub `window`/`document`/`localStorage`，`eval` parser+analyzer，
  載入 `data.json`，呼叫各 analyzer 函式驗證不報錯。
- **修改後務必重新打包**：`node build.js` → 產生 `TITAN-STAR.html`（會自動驗證 9 個
  script 區塊完整、無提前關閉）。
- **部署**：合併/推送到 `main` 即由 GitHub Pages 自動發佈（無 workflow）。

---

## 11. 給其他 AI 的分析切入建議

若要對本專案提出分析或改善建議，最有價值的角度通常是：

- **究責邏輯**：全新/整新與雙批次分析是否正確、製令解析（`parseOrderMonth`）邊界情況。
- **角色 findings 的覆蓋與精準度**：`gatherFindings` 的 `roles` 標註是否合理、是否有
  角色看到的東西太少或太雜。
- **統計方法正確性**：SPC 管制界限、FMEA 的 S/O/D 估算、預測模型在小樣本下的可靠度。
- **資料品質**：機種/零件正規化是否漏接、品號大類對照覆蓋率。
- **可用性**：長輩使用者的資訊密度、字級、操作路徑。

> 注意：分析時請務必先吸收 **§2 核心領域知識**，否則容易誤判「全新/整新空白」「COPQ 空白」
> 「CAPA 空白」為缺陷——它們其實是**資料尚未齊備**的預期狀態，系統設計已預留並有引導提示。
