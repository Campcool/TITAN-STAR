# TITAN-STAR 多人協作整合規劃（M365）— 後續改版建議

**狀態：後續改版建議，尚未排入實作。** 建立於 2026-09-17。

使用者 2026-09-17 決定：**先以現行線上版（靜態站 + localStorage）完成其餘修正**，
多人協作整合列為後續改版。本文件保留完整架構與 IT 申請清單，
待業主決定啟動時可直接照著走，不必重新訪談。

> 接手者注意：在本規劃啟動前，CAPA 與 RMA 仍是**單機 localStorage**，
> 不要對外宣稱或在介面上暗示它們是共用案件平台。
> 現行版本的改動請以「之後搬得動」為原則，避免把儲存邏輯散寫進 UI。

## 使用者已定調的四件事

| 決定 | 內容 |
|---|---|
| 協作方式 | 多人共同追進度與排錯，不是個人分析工具 |
| 平台 | 公司既有 Microsoft 365（Outlook / Teams / SharePoint） |
| 帳號 | 改成真帳號，維修資料不再公開 |
| 規模 | 15 人以上，跨部門含主管層 |

這四項推翻了 AI-HANDOFF「公開曝光現況」段落裡 2026-08-17 的「維持公開現狀」決定。
該段落的四項防回歸（noindex、客戶名稱遮罩、姓名只留姓氏、匯入自動遮罩）仍然有效，
但「data.json 公開可下載」這一項從「已接受的取捨」變成「待修正項目」。

## 現況與落差

目前**沒有任何資料是共用的**：

| 資料 | 現在存哪 | 後果 |
|---|---|---|
| CAPA 改善案 | `localStorage['titan_capa_v1']` | A 建的案子 B 看不到；清快取就沒了 |
| RMA 單 | `localStorage['rma_db_v1']` | 同上 |
| 成本設定 / FMEA 人工調整 / 料件主檔 | localStorage | 每台裝置各一份，永遠不一致 |
| 使用者名單 | `data.json` 的 `users`（唯讀，單向） | 唯一的跨裝置同步，但前端不寫回 |
| 維修分析資料 | `data.json`（公開網址） | 任何人可直接下載 |

登入是純前端：admin 固定 `031780`，其他只要名單裡有就放行、**不驗密碼**。
在這個前提下，改善案就算做了「誰負責、誰結案」也沒有追蹤價值，因為任何人都能冒用任何身分。

## 目標架構

```
瀏覽器（靜態站續留 GitHub Pages）
   │  MSAL.js — Entra ID 登入（SPA + PKCE，無用戶端密碼）
   ▼
Microsoft Graph API
   ├─ SharePoint 文件庫 ── data.json（登入後才拿得到）
   ├─ SharePoint 清單 ──── 改善案（CAPA / 8D）
   ├─ SharePoint 清單 ──── RMA 單
   └─ SharePoint 文件庫 ── 證據附件
權限：Entra 安全性群組（主管 / 品檢 / 維修）
```

### 為什麼站台繼續放 GitHub Pages

要保護的是**資料**，不是程式碼——程式碼本來就公開，也沒有秘密可言
（SPA 不能放任何密鑰，這是架構限制不是疏忽）。把資料移到 Entra ID 後面，
公開的站台就只剩一個空殼，沒登入什麼都拿不到。
這樣既有的 CI、版本錨點、部署流程、離線單檔全部可以沿用，不用搬家。

### 為什麼用 SharePoint 清單而不是自建資料庫

1. **版本歷史是內建的**——清單每一筆的每次修改都有「誰、什麼時候、改了什麼」，
   這正好是「共同追進度」的核心需求，不用自己寫稽核表。
2. **附件有地方放**——證據照片、量測報告直接進文件庫，不用另外找檔案儲存服務。
3. **不新增廠商、不新增月費、不新增要維護的伺服器**——IT 只在既有租戶做設定。
4. **權限用 Entra 群組**——人員異動由 IT 在既有流程處理，系統這邊不用維護第二份名冊。

## 要請 IT 做的事（可直接轉給 IT）

### 1. Entra ID 應用程式註冊

| 項目 | 值 |
|---|---|
| 名稱 | TITAN-STAR 維修分析 |
| 平台類型 | **單頁應用程式（SPA）** |
| 重新導向 URI | `https://campcool.github.io/TITAN-STAR/` |
| 開發用 URI（可選） | `http://localhost:8765/` |
| 用戶端密碼 | **不需要**（公開靜態站不能放密鑰） |
| 支援帳戶類型 | 僅限此組織目錄中的帳戶（單一租戶） |

**需要 IT 回傳給我們兩個值**：應用程式（用戶端）識別碼 `Client ID`、目錄（租戶）識別碼 `Tenant ID`。
這兩個值不是機密，可以寫進前端程式碼。

### 2. API 權限（**委派 delegated**，不要應用程式權限）

| 權限 | 用途 | 備註 |
|---|---|---|
| `User.Read` | 讀登入者姓名與部門，用於顯示與分角色 | 一般不需管理員同意 |
| `Sites.Selected` | 只存取下面那一個 SharePoint 站台 | **不要用 `Sites.ReadWrite.All`**，那會給到整個租戶所有站台 |
| `GroupMember.Read.All` | 判斷使用者屬於哪個角色群組 | 需管理員同意；若 IT 不願意，改用清單維護角色，見下方替代方案 |

以上需要**系統管理員同意（admin consent）**。

### 3. SharePoint 站台

- 建立一個小組網站，例如 `TITAN-STAR`。
- 用 `Sites.Selected` 把上面註冊的應用程式**只授權到這一個站台**
  （Graph 的 site permission，IT 熟悉這個流程）。
- **需要回傳**：站台網址。

### 4. Entra 安全性群組（三個）

| 群組 | 權限 |
|---|---|
| `TITAN-STAR-主管` | 看全部、簽核結案 |
| `TITAN-STAR-品檢` | 開改善案、根因驗證、提送結案 |
| `TITAN-STAR-維修` | 更新維修進度、填證據 |

把 15+ 位使用者分別加入。

### 5. 需要 IT 確認的一件事

手機在**外網**要能用 → 確認條件式存取原則（Conditional Access）不會擋掉
外網瀏覽器登入。MFA 沒問題，我們支援；但若原則限制「只能從公司網路存取」，
外勤與主管在外面就進不來，需要先調整。

### 替代方案（若 IT 不給 `GroupMember.Read.All`）

改用一張 SharePoint 清單維護「員工 → 角色」對照，由品檢主管自行維護。
安全性略低（角色不是由 IT 統一管控），但不卡 IT，其餘設計不變。

## 實作順序

### Phase 0 — 現在就能做，不等 IT

| # | 工作 | 為什麼現在做 |
|---|---|---|
| 0-1 | 抽出資料存取層：CAPA / RMA 的讀寫收斂成一個 adapter 介面，行為完全不變 | 之後換 SharePoint 時不用動 UI，這一步做完換後端只是換一個檔 |
| 0-2 | 重做 CAPA 資料模型：階段（立案→圍堵→根因→對策→驗證→結案）、負責人、期限、根因驗證、效果證據、結案條件、變更歷程 | 多人協作必要；欄位設計直接對應之後 SharePoint 清單的欄位，只做一次 |
| 0-3 | 每個案件加穩定 ID 與「來源指紋」（型號＋期間＋異常類型） | 避免同一個問題被兩個人各開一張，這是多人協作才會出現的問題 |

Phase 0 全部可以在本機做完並驗證，不需要任何 IT 配合。

### Phase 1 — IT 平行進行

上面的「要請 IT 做的事」。這是整條路徑上前置時間最長的一段，越早送越好。

### Phase 2 — 拿到 Client ID 之後

- 2-1 MSAL 登入取代假登入；刪除 `titan_users_v1` 與固定 admin `031780`。
- 2-2 角色改由 Entra 群組決定，移除前端寫死的權限判斷。

### Phase 3 — 資料轉私有

- 3-1 `data.json` 搬到 SharePoint 文件庫，前端改用 Graph 下載。
      **必須做快取**：2.6MB 不能每次開頁重抓。
- 3-2 從 Pages artifact 移除 `data.json`
      （改 `scripts/prepare-pages-artifact.sh` 的 `public_rules` 白名單與 `required_paths`）。
- 3-3 CI 的去識別化檢查（`scripts/mask-identifiers.cjs`）改成在上傳 SharePoint 前掃來源，
      不是掃 repo——因為 repo 裡將不再有 data.json。

### Phase 4 — 共用案件

- 4-1 CAPA adapter 換成 SharePoint 清單，附件進文件庫。
- 4-2 RMA 同上。
- 4-3 進度看板、逾期提醒、指派通知。

### Phase 5 — 驗收

多人同時操作同一筆、越權測試（維修角色不能結案）、手機外網、
離線降級行為、既有分析頁數值與改版前一致。

## 兩個必須先講清楚的風險

### 1. 把 data.json 下架，不等於它沒外流過

它已經公開在 GitHub Pages 上一段時間，而且**git 歷史裡每一版都還在**。
AI-HANDOFF 記載這是 2026-08-17 業主評估後接受的取捨；現在決定改變了，
這個取捨要重新評估。

清 git 歷史是一個獨立且具破壞性的動作（會改寫所有 commit hash、
所有人的本機 clone 都要重來），**需要業主明確決定**，不在本規劃的預設範圍內。
Phase 3 只負責「從今以後不再公開」。

### 2. 離線 / PWA 會降級

現在 service worker 會快取 `data.json` 供離線使用。資料改成登入後才能拿之後，
離線就只剩「上次登入後快取的內容」，且首次使用必須連得上網路。
廠內沒訊號的使用場景要先確認可接受。

## 不在本輪範圍

- 清 git 歷史（見上）。
- 8D 完整 D0–D8 流程——先把 CAPA 的階段與證據做對，8D 共用同一份改善案資料，
  不另開一套（避免同樣內容填兩次）。
- 資訊架構改版（六大分類選單）——與協作無關，另案處理。
